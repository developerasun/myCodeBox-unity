using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIexercise : MonoBehaviour
{
    // use RectTool(command t in canvas) to manipulate UI element size, not scale(command R)
    // use display aspect to anchor UI element in a certain position responsively. 
    // just like event game object has a Transform, every UI element has RectTransform.
    // install 2D sprite package to manipulate responsive 2D image
    // set UI scale mode to scale with screen size in Canvas
    
}
