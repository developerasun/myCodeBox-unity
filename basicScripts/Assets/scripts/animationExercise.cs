using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class animationExercise : MonoBehaviour
{
    // character sometimes needs to be interactive(often interact with other objects), which requires animation
    // the hierachy of game character: 
    // 1) object with animator component 
    // 2) mesh(render character)  
    // 3) character skeleton(rig) 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
